package org.lld.vehicle;

import org.lld.DrivingStrategy;
import org.lld.Vehicle;

public class NormalCar extends Vehicle {

    public NormalCar(DrivingStrategy drivingStrategy)
    {
        super(drivingStrategy);
    }
}
